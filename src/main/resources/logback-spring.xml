<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty name="appName" source="spring.application.name"/>

    <!-- ─────────── local / default 환경 ───────────
         Spring Boot 기본 colored 패턴 사용, OTEL 비활성화 -->
    <springProfile name="local,default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <!-- 로컬에 Kafka 브로커가 안 떠 있을경우 → AdminClient/Consumer 재연결 시도 로그 억제.
             dev/prod 에선 정의하지 않아 진짜 연결 장애는 그대로 보임. -->
        <logger name="org.apache.kafka" level="OFF"/>
        <logger name="org.springframework.kafka" level="OFF"/>
    </springProfile>

    <!-- ─────────── dev / prod 환경 ───────────
         traceId/spanId 포함 + OTEL OTLP export -->
    <springProfile name="dev,prod">
        <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
            <captureExperimentalAttributes>true</captureExperimentalAttributes>
            <captureCodeAttributes>true</captureCodeAttributes>
            <captureMarkerAttribute>true</captureMarkerAttribute>
            <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
            <captureMdcAttributes>*</captureMdcAttributes>
        </appender>

        <appender name="CONSOLE_TRACE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [service=${appName}] [traceId=%X{trace_id:-}] [spanId=%X{span_id:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE_TRACE"/>
            <appender-ref ref="OTEL"/>
        </root>

        <!-- Kafka 브로커 장애 시 NetworkClient 의 매초 재연결 WARN/INFO 도배 제거.
             연결 장애 감지는 /health/infra/kafka(503) + 메트릭 + spring-kafka
             리스너 컨테이너 ERROR 로 한다. 여기서 ERROR 로 올려도 그 경로는 영향 없음. -->
        <logger name="org.apache.kafka.clients.NetworkClient" level="ERROR"/>
        <logger name="org.apache.kafka.clients.admin.internals.AdminMetadataManager" level="ERROR"/>
    </springProfile>

    <!-- 패키지별 레벨 -->
    <logger name="net.watchbox" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
</configuration>
